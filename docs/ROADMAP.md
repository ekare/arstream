# Roadmap

## Milestones (active development)

| # | Stage | Status |
|---|---|---|
| M0 | Device capability discovery spike | Code-free portion done (see `DEVICE_COMPATIBILITY.md`); runtime `ArCoreApk_checkAvailability()` + sensor timestamp drift test deferred to M6/M2 (ARCore's C API can't be called without the JNI bootstrap) |
| M1 | GDExtension skeleton (Android arm64) | **Done** — built with SCons+godot-cpp+NDK 29, Godot 4.6.1 export toolchain (export template + JDK + debug keystore) set up, APK installed on A30s, ping/pong round-trip verified |
| M2 | Camera2 fallback capture | **Done** (IMU sampling excluded — not yet added) — NDK Camera2 writes directly (zero-copy) to the encoder's input surface, proven on the A30s |
| M3 | AMediaCodec H.264 encode (local file) | **Done** — `OutputSink` architecture with "save"/"stream" options; `FileSink` produced ~797 frames (14 SPS/PPS+IDR cycles, 783 P-frames) of a valid Annex-B stream on the A30s, NAL structure verified with Python; `StreamSink` is a deliberate stub (M4/M5) |
| M4 | Protocol v1 (C++ + Python in parallel) | **Done** — `net/protocol.{h,cpp}` (C++, no godot-cpp dependency, builds on host+Android+Windows) and `arstream_server.protocol` (Python) tested against `fixtures/*.bin` golden-byte files (34 C++ checks + 16 pytest, both 0 failures). There's an intentional asymmetry in JSON-bodied messages: Python does full encode/decode, C++ verifies framing but doesn't parse the JSON itself (to stay dependency-free) — see the note at the top of `protocol.h`. `StreamSink` was still waiting on M5. |
| — | UI: portrait mode, top-left overlay, bottom-center async preview toggle | **Done** — a second (640x360) `AImageReader` output from the same capture session, only processed on CPU while `preview_enabled`; verified on the A30s via screenshot that it toggles on/off without affecting recording fps and shows a REAL camera image. Two bugs were found and fixed along the way: (1) `preview_texture_.unref()` was releasing ArCapture's own reference, not the object held by TextureRect — the screen stayed blank the whole time; (2) the camera sensor is physically mounted rotated 90° (`ACAMERA_SENSOR_ORIENTATION`, queried from the device and confirmed) but this was never compensated for — the preview is now actually rotated, and a `rotation` metadata field was added to `VIDEO_CONFIG` on the recording/streaming side too (metadata, not pixels, gets rotated — the same method standard video containers use) |
| M5 | End-to-end same-LAN streaming test | **Done** — `StreamClient` (POSIX socket) + `StreamSink` (memory-first/disk-overflow buffer, resilient to TCP drops) tested on a real WiFi LAN from the A30s to a laptop: during a deliberate ~25s server outage, memory filled at 8MB and overflowed to disk (`stream_spool.bin` grew to 16.8MB), then drained fully in order (no loss) once the server came back, and the spool file was deleted; `VIDEO_CONFIG` is resent on every (re)connection (two real bugs were found and fixed during testing: a race where the connection could be established before the encoder had produced SPS/PPS, and an unbounded `join()` on shutdown — now capped at 5s) |
| — | UI: red recording-indicator dot + preview available outside of recording | **Done** — a red dot (`RecordingIndicator`) in the top-right corner, visible only while `capturing_`. Preview is now fully independent of recording/streaming: `Camera2CaptureSession::start()` can now also be opened with `encoder_surface=nullptr` (TEMPLATE_PREVIEW, preview target only) — with no recording active, `ArCapture::set_preview_enabled()` opens/closes its own camera session; when recording starts, the existing session is closed and the encoder-backed one is opened while the preview flag is preserved. Verified on the A30s: preview on/off while idle (clean CAMERA_STATE_ACTIVE/CLOSED transitions), red dot + uninterrupted preview during recording, dot disappearing and preview auto-resuming (while the toggle stays on) once recording stops — no crashes/errors in any case. |
| — | Plugin-based web dashboard (`server/`) | **Done** — `ingest.py` now dispatches to a `PluginManager` instead of raw logging; built-in `RecorderPlugin` (writes `video.h264`+`meta.json` to disk) and `StatsPlugin` (counters + `EventBus` publishing); FastAPI/uvicorn dashboard runs in the same asyncio loop (`/api/sessions`, `/api/sessions/{id}/download` — in-memory `.zip` on the fly, `/ws/live` for live updates). Verified end-to-end with a synthetic client (TCP ingest → plugin → EventBus → WebSocket → dashboard table, and that the downloaded `.zip` isn't corrupted, via `zipfile.testzip()`), 48 pytest tests pass. Details: [`server/README.md`](../server/README.md). |
| M6 | ARCore capture path (pose/point-cloud/intrinsics) | Not started — once real-device verification on ARCore-supported hardware is settled |
| M7 | GitHub-quality polish + CI | Not started |

## Deferred (v2 / next phase, not being built now)

- **iOS/ARKit support** — via an `.mm` (Objective-C++) bridge: `arkit_capture_session.mm`, `avf_capture_session.mm`, `imu_sampler_ios.mm`. No Mac and no test device yet. See [`docs/IOS_HANDOVER.md`](IOS_HANDOVER.md) for where to start once a Mac is available, which files can be reused unchanged (protocol, `StreamSink`, `StreamClient` — all already platform-independent), and the open unknowns. `stream_client.cpp`'s POSIX socket code has already been opened up for `IOS_ENABLED` too (verified via the Android build; it needed nothing iOS-specific).
- **v2 transport layer: UDP + lightweight/fast encryption** — the same message format over UDP (the `sequence_number` field in the header is already reserved for this), with loss/ordering handled manually; needed once NAT-behind/lossy internet connections and TCP's head-of-line blocking start affecting video latency. Encryption should be a low-latency scheme instead of full TLS — a lightweight handshake + ChaCha20-Poly1305 (or a similar AEAD) — not yet designed, just noted as a direction. QUIC (UDP+TLS1.3 already combined) is also a candidate that solves both problems in one step.
- **v1's current boundary** — TCP-only, no TLS/authentication, assumes a trusted network (see `PROTOCOL.md` §6). Recommendation until this is addressed: a WireGuard/Tailscale/SSH tunnel instead of bolting crypto onto the protocol.
- **32-bit (`armv7`) target build** — could be considered for very old devices; the A30s is already arm64, not a priority.
- **Software H.264 encoder fallback** — a device with no hardware AVC encoder is considered rare; will be addressed if one actually turns up.
